% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_taxonomic_update_lookup.R
\name{create_taxonomic_update_lookup}
\alias{create_taxonomic_update_lookup}
\title{Align and update a list of taxon names in one call}
\usage{
create_taxonomic_update_lookup(
  original_name,
  resources = NULL,
  identifier = NA_character_,
  fuzzy_abs_dist = 3,
  fuzzy_rel_dist = 0.2,
  fuzzy_matches = TRUE,
  imprecise_fuzzy_matches = FALSE,
  taxon_ranks_to_check = NULL,
  hybrids = FALSE,
  intergrades_affinis = FALSE,
  progress = FALSE,
  full = FALSE
)
}
\arguments{
\item{original_name}{Character vector of raw taxon names to align and update.}

\item{resources}{The nested list of reference tibbles produced by
\code{\link[=prepare_taxonomic_resources]{prepare_taxonomic_resources()}}. A plain (already fully-formatted) taxonomic reference tibble is
also accepted directly -- see \code{align_taxa()}'s \code{resources} documentation.}

\item{identifier, fuzzy_abs_dist, fuzzy_rel_dist, fuzzy_matches, imprecise_fuzzy_matches, taxon_ranks_to_check, hybrids, intergrades_affinis, progress}{Forwarded to \code{\link[=align_taxa]{align_taxa()}}; see its documentation.}

\item{full}{Logical; if \code{TRUE}, return every intermediate column \code{\link[=align_taxa]{align_taxa()}}/\code{\link[=update_taxa]{update_taxa()}}
compute. If \code{FALSE} (the default), return just the key output columns: \code{original_name},
\code{aligned_name}, \code{accepted_name}, \code{suggested_name}, \code{genus}, \code{family}, \code{taxon_rank},
\code{taxonomic_dataset}, \code{taxonomic_status}, \code{taxonomic_status_aligned}, \code{aligned_reason} and
\code{update_reason}.}
}
\value{
A tibble with one row per element of \code{original_name}; see \code{\link[=update_taxa]{update_taxa()}} for column
details.
}
\description{
The one-call convenience pipeline: \code{\link[=align_taxa]{align_taxa()}} followed by \code{\link[=update_taxa]{update_taxa()}}. Matches each name in
\code{original_name} against \code{resources}, then resolves that match forward to its current accepted name.
}
