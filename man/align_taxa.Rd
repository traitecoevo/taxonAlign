% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/align_taxa.R
\name{align_taxa}
\alias{align_taxa}
\title{Align raw taxon names to a prepared taxonomic reference}
\usage{
align_taxa(
  original_name,
  resources,
  identifier = NA_character_,
  fuzzy_abs_dist = 3,
  fuzzy_rel_dist = 0.2,
  fuzzy_matches = TRUE,
  imprecise_fuzzy_matches = FALSE,
  taxon_ranks_to_check = NULL,
  full = FALSE
)
}
\arguments{
\item{original_name}{Character vector of raw taxon names to align.}

\item{resources}{The nested list of reference tibbles produced by
\code{\link[=prepare_taxonomic_resources]{prepare_taxonomic_resources()}}.}

\item{identifier}{A dataset, location or other identifier associated with each name in
\code{original_name} -- used only to disambiguate \verb{genus sp.}-style aligned names (see
\code{\link[=prepare_taxonomic_resources]{prepare_taxonomic_resources()}}/\code{match_taxa()}) as belonging to a specific dataset/location.
Recycled if length 1; otherwise must be the same length as \code{original_name}. Defaults to \code{NA}.}

\item{fuzzy_abs_dist}{The number of characters allowed to differ for a fuzzy match.}

\item{fuzzy_rel_dist}{The proportion of characters allowed to differ for a fuzzy match.}

\item{fuzzy_matches}{Logical; whether fuzzy matching is attempted at all. Defaults to \code{TRUE}.}

\item{imprecise_fuzzy_matches}{Logical; reserved for a more lenient fuzzy-matching pass (not yet
implemented -- see \code{match_taxa()}). Defaults to \code{FALSE}.}

\item{taxon_ranks_to_check}{Optional character vector of taxonomic ranks (besides species) to
attempt higher-rank matches against. Defaults to \code{NULL}, which uses every higher rank present in
\code{resources}.}

\item{full}{Logical; if \code{TRUE}, return every intermediate column \code{match_taxa()} computes (useful
for inspecting \emph{why} a name matched the way it did). If \code{FALSE} (the default), return just the
key output columns.}
}
\value{
A tibble with one row per element of \code{original_name} (preserving its length, order and any
duplicates/\code{NA}s), with columns \code{original_name}, \code{cleaned_name}, \code{aligned_name},
\code{taxonomic_dataset}, \code{taxon_rank}, \code{taxonomic_status}, \code{aligned_reason}, \code{alignment_code} and
\code{identifier} (or, if \code{full = TRUE}, every column \code{match_taxa()} computes along the way).
}
\description{
Attempts to match each name in \code{original_name} to a name in \code{resources} (produced by
\code{\link[=prepare_taxonomic_resources]{prepare_taxonomic_resources()}}), trying, in order, exact matches (scientific name with
authorship, then canonical name), then progressively more lenient fuzzy matches, at species level
first and then at each higher taxonomic rank present in \code{resources}. See \code{match_taxa()} for the
full sequence of match attempts.
}
