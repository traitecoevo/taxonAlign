% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_taxonomic_reference_list.R
\name{generate_taxonomic_reference_list}
\alias{generate_taxonomic_reference_list}
\title{Generate a taxonomic reference list from the GBIF backbone}
\usage{
generate_taxonomic_reference_list(
  taxon_name,
  name_rank = NULL,
  name_kingdom = NULL,
  country = NULL,
  rank = NULL,
  include_synonyms = TRUE,
  cache_dir = tools::R_user_dir("taxonAlign", "cache"),
  refresh_cache = FALSE,
  max_cache_age_days = 30,
  facet_limit = 1e+05,
  max_taxa = 50000,
  force_large_fetch = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{taxon_name}{Character vector of one or more taxon names to build the
reference list from (e.g. \code{"Rutaceae"}, \code{"Boronia"}, \code{"Lepidoptera"}). Every rank
below each name is included; use \code{rank} to trim that back.}

\item{name_rank}{Optional character, the taxonomic rank of \code{taxon_name}
(e.g. \code{"genus"}, \code{"family"}). Only needed to disambiguate homonyms that exist at
more than one rank; passed to \code{\link[rgbif:name_backbone]{rgbif::name_backbone()}}. Recycled against
\code{taxon_name} if both have length > 1.}

\item{name_kingdom}{Optional character, the kingdom of \code{taxon_name} (e.g.
\code{"Plantae"}, \code{"Animalia"}). Only needed to disambiguate homonyms that exist in more
than one kingdom (a common case -- e.g. the plant genus \emph{Eremophila} and the bird
genus \emph{Eremophila} -- that \code{name_rank} alone cannot resolve). Recycled against
\code{taxon_name} if both have length > 1.}

\item{country}{Optional ISO 3166-1 alpha-2 country code (e.g. \code{"AU"}). When
supplied, the list is restricted to taxa with at least one GBIF occurrence record
in that country.}

\item{rank}{Optional character, a rank from GBIF's rank enum (e.g. \code{"genus"},
\code{"species"}). When supplied, only taxa at this rank or below (i.e. more specific
ranks, such as species and subspecies under \code{"genus"}) are returned.}

\item{include_synonyms}{Logical; if \code{FALSE}, only accepted names are returned.
Defaults to \code{TRUE}.}

\item{cache_dir}{Directory used to cache downloaded taxonomy/occurrence data.
Defaults to a per-user cache directory (see \code{\link[tools:R_user_dir]{tools::R_user_dir()}}) so the cache
persists across sessions.}

\item{refresh_cache}{Logical; if \code{TRUE}, ignore any cached data and re-download
from GBIF. Defaults to \code{FALSE}.}

\item{max_cache_age_days}{Numeric; cached data older than this many days is
treated as stale and re-downloaded. Defaults to 30.}

\item{facet_limit}{Numeric; the maximum number of distinct taxa to request when
looking up which taxa occur in \code{country}. Increase this if a \code{warning} reports the
result may be truncated. Defaults to 100000.}

\item{max_taxa}{Numeric; refuse to download a taxonomic tree with more than this
many taxa unless \code{force_large_fetch = TRUE}. This is mainly a safety net for
ambiguous names -- GBIF resolves some homonyms to their common ancestor (e.g. a
whole kingdom) rather than erroring -- but also protects against accidentally
requesting a very slow, very large first-time download. Defaults to 50000.}

\item{force_large_fetch}{Logical; set to \code{TRUE} to proceed with a fetch that would
otherwise be blocked by \code{max_taxa}. Defaults to \code{FALSE}.}

\item{quiet}{Logical; suppress progress messages. Defaults to \code{FALSE}.}
}
\value{
A tibble with one row per taxon, with column names matching those used by
\href{https://traitecoevo.github.io/APCalign/}{APCalign} (\code{traitecoevo/APCalign}) wherever an
equivalent concept exists there, so this reference list can be combined with an
APC/APNI-derived one: \code{taxon_ID} (the GBIF backbone usageKey), \code{parent_key} (GBIF-specific;
no APCalign equivalent), \code{accepted_name_usage_ID} (the \code{taxon_ID} of the accepted usage --
equal to \code{taxon_ID} itself for a row that is already accepted, matching how APC downloads
fill this in), \code{scientific_name} (the full name, with authorship), \code{scientific_name_authorship},
\code{canonical_name} (the name without authorship), \code{taxon_rank}, \code{taxonomic_status}, \code{kingdom},
\code{phylum}, \code{class}, \code{order}, \code{family}, \code{genus} and \code{taxonomic_dataset} (always \code{"GBIF"}).
}
\description{
Builds a table of taxon names sourced from the
\href{https://www.gbif.org/dataset/d7dddbf4-2cf0-4f39-9b2a-bb099caae36c}{GBIF backbone taxonomy},
optionally restricted to taxa recorded as occurring in a given country and/or to a
minimum taxonomic rank.
}
\details{
The whole taxonomic tree below \code{taxon_name} is fetched once (in large, paginated
batches, rather than one API call per name) and cached to disk, so repeat calls --
including calls that filter by a different \code{country} or \code{rank} -- are served from
the cache and return almost instantly. Use \code{refresh_cache = TRUE} to force a fresh
download, e.g. once the cache is more than a few weeks old.
}
\examples{
\dontrun{
# every taxon below the genus Boronia
generate_taxonomic_reference_list("Boronia")

# species (and infraspecific taxa) of Boronia recorded in Australia
generate_taxonomic_reference_list("Boronia", country = "AU", rank = "species")

# combine several families in one call
generate_taxonomic_reference_list(c("Rutaceae", "Sapindaceae"), country = "AU")
}

}
